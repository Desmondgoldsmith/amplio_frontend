---
import Layout from '../layouts/Layout.astro';
import TeamMemberCard from '../components/Team/TeamMemberCard.astro';
import { teamMembers } from '../constants';

---

<Layout title="Our Team | Amplio | Talking Books">
  <main class="w-full bg-[#F2FAFE] py-6">
    <div class="max-w-4xl mx-auto space-y-12 px-4 sm:px-6 lg:px-8">
      <div class="text-center space-y-4">
        <h1 class="text-4xl font-bold text-gray-900 mt-4">Our Team</h1>
        <p class="text-gray-600 max-w-3xl mx-auto px-4 sm:px-0">
          Amplio Ghana has its headquarters in Wa, in the Upper West Region of Ghana, and 
          a branch in Tamale, Northern Region. We are guided by our values of partnership, 
          responsibility, respect, transparency, and learning.
        </p>
      </div>

      <div class="space-y-8">
        {teamMembers.map(member => (
          <TeamMemberCard
            name={member.name}
            role={member.role}
            image={member.image}
            bio={member.bio}
          />
        ))}
      </div>
    </div>
  </main>
</Layout>
