---
import { Image } from "astro:assets";
import AmplioLabel from "../../assets/techAssets/amplio-lable.png";
import AmplioColors from "../../assets/techAssets/amplio-colors.png";
import GreenAmplio from "../../assets/techAssets/green-amplio.png";
import Women from "../../assets/techAssets/women.jpg";
---

<section class="bg-white py-20">
    <div class="max-w-7xl mx-auto px-4 md:px-6 lg:px-8">
      {/* Header Section */}
      <div class="text-center max-w-3xl mx-auto mb-16">
        <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-6">
          Designed with the user
        </h2>
        <p class="text-base lg:text-lg text-gray-700">
          The Amplio Talking Book was designed with and for low-literate people in rural, 
          remote, and off-grid communities. It uses audio prompts and visual icons to 
          guide the user. Each icon is embossed with a dot for the visually impaired.
        </p>
      </div>
  
      {/* Main Device Diagram */}
      <div class="mb-20">
        <div class="max-w-2xl mx-auto">
          <Image 
            src={AmplioLabel}
            alt="Amplio Talking Book device features diagram"
            width={800}
            height={600}
            class="w-full h-auto"
          />
        </div>
      </div>
  
      {/* Features Section */}
      <div class="grid md:grid-cols-2 gap-16 max-w-5xl mx-auto">
        {/*  Additional Features */}
        <div>
          <div class="mb-8">
            <Image 
              src={AmplioColors}
              alt="Amplio Talking Book color variants"
              width={500}
              height={400}
              class="w-full max-w-md mx-auto"
            />
          </div>
          <div>
            <h3 class="text-xl font-bold text-gray-900 mb-4">Additional features:</h3>
            <ul class="space-y-3 text-gray-700">
              <li>USB port for smartphone updates</li>
              <li>Silicon band for more durability</li>
              <li>Auxiliary port for headphones</li>
            </ul>
          </div>
        </div>
  
        {/* Power Options */}
        <div>
          <div class="mb-8">
            <Image 
              src={GreenAmplio}
              alt="Amplio Talking Book battery options"
              width={500}
              height={400}
              class="w-full max-w-md mx-auto"
            />
          </div>
          <div>
            <h3 class="text-xl font-bold text-gray-900 mb-4">Flexible power options:</h3>
            <ul class="space-y-3 text-gray-700">
              <li>Includes a rechargeable battery and also works with AA or D batteries</li>
              <li>Recharge with grid or solar power</li>
              <li>Connect to rechargeable outdoor speaker</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="relative w-full h-screen">
    <div class="absolute inset-0">
      <Image 
        src={Women} 
        alt="Women in traditional dress sitting under a wooden structure"
        width={1920}
        height={1080}
        class="w-full h-full object-cover"
      />
    </div>
  
    {/* Contained Quote Box */}
    <div class="absolute bottom-[7%] left-4 md:left-12 lg:left-56">
      <div class="bg-[#4D5696]/80 p-4 md:p-8 max-w-[700px]">
        <blockquote class="text-white w-full md:w-[450px]">
          <p class="text-lg md:text-xl lg:text-2xl leading-relaxed mb-4 md:mb-6">
            "After we did a quick Talking Book demo, young people were surprised 
            to see even a 90-year-old woman pick up a device and turn it on."
          </p>
          <footer class="text-base md:text-lg">
            Constance Teague, Land Tenure Specialist, Landesa
          </footer>
        </blockquote>
      </div>
    </div>
  </section>