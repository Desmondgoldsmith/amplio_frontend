---
interface FormData {
  email: string;
  firstName: string;
  lastName: string;
}

const handleSubmit = async (data: FormData) => {
};
---

<section class="relative w-full">
  {/* UNICEF Quote Section */}
  <div class="max-w-[2000px] mx-auto px-44 py-20 text-center">
    <img 
      src="/UNICEF_Logo.png" 
      alt="UNICEF Logo" 
      class="mx-auto mb-12 w-48"
    />
    
    <blockquote class="max-w-4xl mx-auto mb-6 text-2xl font-medium text-gray-800">
      "An innovative technology to influence behaviors that impact the lives and opportunities of the world's most vulnerable children and their families."
    </blockquote>
    
    <div class="text-gray-600">
      <p>Susan Ngongi Namondo, United Nations Diplomat</p>
      <p>and former UNICEF Representative to Ghana</p>
    </div>
  </div>

  <div class="relative w-full h-[600px]">
    {/* Background Image */}
    <div class="absolute inset-0 w-full h-full">
      <img 
        src="/unicef_section.jpg" 
        alt="Community members" 
        class="w-full h-full object-cover"
      />
    </div>

    {/* Form Container */}
    <div class="absolute left-44 top-1/2 -translate-y-1/2 w-[460px] p-12 bg-white bg-opacity-90 backdrop-blur-sm rounded-lg shadow-lg z-10">
      <h3 class="text-3xl font-bold mb-6">Knowledge is power</h3>
      <p class="text-gray-700 mb-8">
        Join us, and help unlock the power of people and communities through knowledge sharing.
      </p>

      <form class="space-y-6">
        <div class="space-y-4">
          <div class="grid grid-cols-2 gap-4">
            <div>
              <input 
                type="text"
                name="firstName"
                placeholder="First Name"
                class="w-full px-4 py-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                required
              />
            </div>
            <div>
              <input 
                type="text"
                name="lastName"
                placeholder="Last Name"
                class="w-full px-4 py-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                required
              />
            </div>
          </div>
          <input 
            type="email"
            name="email"
            placeholder="Email Address"
            class="w-full px-4 py-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent"
            required
          />
        </div>

        <button 
          type="submit"
          class="w-full py-3 px-6 bg-[#FF5C35] hover:bg-[#FF4415] text-white font-medium rounded-md transition-colors duration-200"
        >
          DONATE
        </button>
      </form>
    </div>
  </div>
</section>

